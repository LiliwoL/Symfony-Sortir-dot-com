{% extends 'base.html.twig' %}

{% block title %}Ville index{% endblock %}

{% block body %}

    <h1 class="text-center mb-5" >Gérer les lieux</h1>

    <fieldset class="form-group border p-3">

        <legend class="w-auto px-2"> Filtrer les lieux</legend>
        {{ form(form) }}
    </fieldset>
    <div class="form-groupe my-4">
        <table class="table">
            <thead>
                <tr>
                    <th>Nom</th>
                    <th>CodePostal</th>
                    <th>actions</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
            {% for ville in villes %}
                <tr>
                    <td>{{ ville.nom }}</td>
                    <td>{{ ville.codePostal }}</td>
                    <td>
                        <a href="{{ path('app_ville_edit', {'id': ville.id}) }}">Modifier</a>
                    </td>
                    <td>
                        {{ include('ville/_delete_form.html.twig') }}
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <a href="{{ path('app_ville_new') }}"> <h3>Ajouter</h3></a>
{% endblock %}

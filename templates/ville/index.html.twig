{% extends 'base.html.twig' %}

{% block title %}Ville index{% endblock %}

{% block body %}
    <h1><center>Gérer les lieux</center></h1><br/><br/><br/>
    <h2>Filtrer les lieux</h2>

    {{ form_start(form) }}
    {{ form_end(form) }}


    <table class="table">
        <thead>
            <tr>

                <th>Nom</th>
                <th>CodePostal</th>
                <th>actions</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for ville in villes %}
            <tr>

                <td>{{ ville.nom }}</td>
                <td>{{ ville.codePostal }}</td>
                <td>

                    <a href="{{ path('app_ville_edit', {'id': ville.id}) }}">Modifier</a>

                </td>
                <td>
                    {{ include('ville/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_ville_new') }}"> <h3>Ajouter</h3></a>
{% endblock %}

{% for message in app.session.flashbag.get('message') %}
    <div class="d-flex flex-row justify-content-center m-5 blink_parent">
        <span class="alert alert-warning blink">
            {{ message }}
        </span>
    </div>
{% endfor %}

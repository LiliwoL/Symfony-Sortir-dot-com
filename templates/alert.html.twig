{% for message in app.flashes('succès') %}
    <div class="d-flex flex-row justify-content-center m-5">
         <span class="alert alert-success" role="alert">
            {{ message  }}
        </span>
    </div>
{% endfor %}

{% for erreur in app.flashes('erreur') %}
    <div class="d-flex flex-row justify-content-center m-5">
        <span class="alert alert-danger" role="alert">
            {{ erreur }}
        </span>
    </div>
{% endfor %}
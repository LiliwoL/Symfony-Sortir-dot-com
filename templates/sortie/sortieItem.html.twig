<!--
<td>{{ sortie.dateEnregistrement ? sortie.dateEnregistrement|date('Y-m-d H:i:s') : '' }}</td>
<td>{{ sortie.dateOuvertureInscription ? sortie.dateOuvertureInscription|date('Y-m-d H:i:s') : '' }}</td>
<td>{{ sortie.dateFermetureInscription ? sortie.dateFermetureInscription|date('Y-m-d H:i:s') : '' }}</td>
<td>{{ sortie.dateDebutSortie ? sortie.dateDebutSortie|date('Y-m-d H:i:s') : '' }}</td>
<td>{{ sortie.dateFinSortie ? sortie.dateFinSortie|date('Y-m-d H:i:s') : '' }}</td>
<td>{{ sortie.organisateur.prenom }} {{ sortie.organisateur.nom }} </td>
<a href="{{ path('app_sortie_show', {'id': sortie.id}) }}">Afficher</a>
<a href="{{ path('app_sortie_edit', {'id': sortie.id}) }}">Modifier</a>
-->


<div class="card" style="width: 18rem;">
    <div class="card-body">
        <h5 class="card-title">{{ sortie.nom }}</h5>
        {% if sortie.estInscrit %}
            <h6 style="color: green;" class="action_sortie_participation" data-sortie-id="{{ sortie.id }}">Je participe</h6>
        {% else %}
            <h6 style="color: red;" class="action_sortie_participation" data-sortie-id="{{ sortie.id }}">Je ne participe pas</h6>
        {% endif %}
        <h6 class="card-subtitle mb-2 text-muted">{{ sortie.organisateur.prenom }} {{ sortie.organisateur.nom }}</h6>
        <h6 class="card-subtitle mb-2 text-muted">
            <span class="action_sortie_nb_inscrit" data-sortie-id="{{ sortie.id }}">{{ sortie.nbInscrit }}</span> / {{ sortie.nbInscriptionMax }}
        </h6>
        <p>Description : {{ sortie.description | slice (0,30) }}</p>
        <p>Etat : {{ sortie.etat }}</p>

        <p>{{ sortie.isAnnulee ? 'Oui' : 'Non' }}</p>
        <a href="javascript:void(0);"><span class="action_sortie_sinscrire" data-sortie-id="{{ sortie.id }}">
            {% if sortie.estInscrit %}
                Se désister
            {% else %}
                S'inscrire
            {% endif %}
        </span></a>
    </div>
</div>

{% extends 'baseAdmin.html.twig' %}

{% block title %}{{ parent() }} | Ajouter un membre{% endblock %}

{% block body %}
    {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}

    <h1>S'enregistrer</h1>

    {{ form_start(registrationUtilisateurForm) }}
        <button type="submit" class="btn">Valider votre inscription</button>
    {{ form_end(registrationUtilisateurForm) }}
{% endblock %}

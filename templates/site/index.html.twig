{% extends 'base.html.twig' %}

{% block title %}Site index{% endblock %}

{% block body %}
    <h1 class="text-center mb-5" >Gérer les Sites</h1>
        <fieldset class="form-group border p-3">

            <legend class="w-auto px-2"> Filtrer les sites</legend>

            {{ form(form) }}

        </fieldset>
    <div class="form-groupe my-4">
        <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>actions</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for site in sites %}
            <tr>
                <td>{{ site.nom }}</td>
                <td>
                    <a href="{{ path('app_site_edit', {'id': site.id}) }}">Modifier</a>
                </td>
                <td>
                    {{ include('site/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    </div>
    <a href="{{ path('app_site_new') }}"><h1>Ajouter</h1></a>

{% endblock %}
